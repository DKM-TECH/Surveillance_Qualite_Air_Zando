services:
  - type: web
    name: surveillance-qualite-air-zando
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn appFirebasebien:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11